import React, {Component} from 'react';
import {
    TextInput,
    View
} from 'react-native';

export default class SocialButton extends Component {
    render() {
        return (
            <View>
                <TextInput/>
            </View>
        );
    }
}